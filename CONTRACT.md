# Контракт модуля хранения

## Функция save
- Параметры: tasks (список словарей)
- Возвращает: ничего
- Что делает: сохраняет задачи в файл

## Функция load
- Параметры: нет
- Возвращает: список задач (пустой, если файла нет)